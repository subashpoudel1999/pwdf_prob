services:
  - type: web
    name: pwdf-prob-api
    runtime: python
    rootDir: assets/backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
